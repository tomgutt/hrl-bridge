# TMS — credentials live here only; never reach the workflow.
TMS_HOST=tramway.proxy.rlwy.net
TMS_PORT=17159
TMS_TOKEN=hr-fde-tomguttermann-2026

# Bridge — Bearer token required on every inbound HTTP call.
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
BRIDGE_TOKEN=

# Operational tuning (defaults are sane for the POC)
TMS_READ_TIMEOUT_S=30
TMS_RETRY_ATTEMPTS=3
TMS_BACKOFF_INITIAL_MS=250
LOG_LEVEL=info
